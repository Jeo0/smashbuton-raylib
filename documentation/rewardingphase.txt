
#include <iostream>
#include <vector>
#include <string>

// function declaration and function prototypes
void readyMode();
void coolDownMode();
void showDownMode();
void rewardingPhase();

int main()
{
	readyMode();
	coolDownMode();
	showDownMode();
	rewardingPhase();
}


// function definitions
void readyMode(){}
void coolDownMode(){}
void showDownMode(){}

// cinopy paste ko lang 
void rewardingPhase(){
	int players;
	int rounds;

	std::vector<int> scores(players);


	for ( int rounds = 1; rounds rounds number;	rounds++ ){
		cout << "\nrounds: " << rounds << endl;

		for ( int players e; player < players;	players++ ){
			scores [players] += input_word. length();

			cout << "\nplayer" << ( players + 1 ) << "Points:	" << scores [players] << endl;

		}
	}
}
